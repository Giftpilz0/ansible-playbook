---
######################################
### vars

- name: Talk to localhosts just so we can learn about them
  hosts: localhost

######################################
### roles

- name: Execute role - git
  hosts: localhost
  roles:
    - role: giftpilz0.general.git
